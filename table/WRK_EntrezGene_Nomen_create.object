#!/bin/csh -f

#
# History
#
# 04/08/2004	lec
#	- new
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table WRK_EntrezGene_Nomen (
	taxID                   int             not null,
	_Marker_key		int             not null,
	geneID                  varchar(30)     not null,
	mgiSymbol               varchar(50)     not null,
	mgiName                 varchar(255)    not null,
	egSymbol                varchar(50)     not null,
	egName                  varchar(255)    not null
)
go

checkpoint
go

quit

EOSQL
