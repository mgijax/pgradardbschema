#!/bin/csh -f

#
# History
#
# 12/21/2004	dbm
#	- new
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table DP_MGC_Clones (
	_MGCClone_key                int             not null,
	imageID                      varchar(30)     null,
	mgcID                        varchar(30)     null,
	cloneStatus                  varchar(40)     null,
	organism                     varchar(40)     null,
	library                      varchar(255)    null,
	seqID                        varchar(30)     null,
	length                       int             null,
	cdsLength                    int             null,
	locusLinkID                  varchar(30)     null,
	unigeneID                    varchar(30)     null,
	defLine                      varchar(255)    null,
	_JobStream_key               int             not null,
	creation_date                datetime        not null
)
go

checkpoint
go

quit

EOSQL
