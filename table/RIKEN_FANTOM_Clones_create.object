#!/bin/csh -f

#
# History
#
# 02/19/2004	dbm
#	- new
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table RIKEN_FANTOM_Clones (
	cloneID                        varchar(30)     not null
)
go

checkpoint
go

quit

EOSQL
