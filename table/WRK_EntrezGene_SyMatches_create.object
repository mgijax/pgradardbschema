#!/bin/csh -f

#
# History
#
# 05/02/2011	lec
#	- was originally a "tempdb..symatches"; createing a "real" table
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table WRK_EntrezGene_SyMatches (
	geneID                  varchar(30)     not null,
	mgiID    		varchar(30) 	not null
)
go

checkpoint
go

quit

EOSQL
