#!/bin/csh -f

#
# History
#
# 01/12/2004	dbm
#	- new
#
# 03/08/2004	dbm
#	- replace unique _Library_key with non-unique libraryNum
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table MGI_CloneLibrary (
	libraryNum                     int             not null,
	cloneSet                       varchar(80)     not null,
	goodName                       varchar(255)    not null,
	badName                        varchar(255)    not null
)
go

checkpoint
go

quit

EOSQL
