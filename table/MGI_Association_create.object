#!/bin/csh -f

#
# History
#
# 01/19/2005	dbm
#	- new
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table MGI_Association (
	_Assoc_key                     int             not null,
	_Record_key                    int             not null,
	accID                          varchar(30)     not null,
	logicalDB                      varchar(80)     not null,
	target                         bit             not null,
	_JobStream_key                 int             not null,
	creation_date                  datetime        not null
)
go

checkpoint
go

quit

EOSQL
