#!/bin/csh -f

#
# History
#
# 04/08/2004	lec
#	- new
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table WRK_EntrezGene_EGSet (
	taxID                   integer         not null,
	geneID    		varchar(30) 	not null,
	compareID  		varchar(30) 	not null,
	idType			varchar(10) 	not null
)
go

checkpoint
go

quit

EOSQL
