#!/bin/csh -f

#
# History
#
# 04/28/2005	lec
#	- new
#

cd `dirname $0` && source ./Configuration

cat - <<EOSQL | ${MGI_DBUTILS}/bin/doisql.csh ${DBSERVER} ${DBNAME} $0

use ${DBNAME}
go

create table DP_EntrezGene_MIM (
	mimID           varchar(30)     not null,
	geneID          varchar(30)     not null,
	annotationType  varchar(30)     not null,
	source          varchar(30)     not null,
	medgencui       varchar(30)     not null
)
go

checkpoint
go

quit

EOSQL
